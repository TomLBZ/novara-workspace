{
  "_comment": "Route schema for ws-gateway. Longest prefix wins; /healthz is reserved by the router. This example is what a fresh clone serves: the hello-world example site.",
  "listen": [80, 8081],
  "routes": [
    { "prefix": "/", "type": "static", "root": "services/sites/hello" }
  ]
}
